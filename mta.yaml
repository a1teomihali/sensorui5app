_schema-version: "3.2"
ID: devuiappsensormanager
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: devuiappsensormanager-destination-content
  type: com.sap.application.content
  requires:
  - name: devuiappsensormanager-destination-service
    parameters:
      content-target: true
  - name: devuiappsensormanager-repo-host
    parameters:
      service-key:
        name: devuiappsensormanager-repo-host-key
  - name: devuiappsensormanager-uaa
    parameters:
      service-key:
        name: devuiappsensormanager-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: devuiappsensormanager_html_repo_host
          ServiceInstanceName: devuiappsensormanager-html5-srv
          ServiceKeyName: devuiappsensormanager-repo-host-key
          sap.cloud.service: devuiappsensormanager
        - Authentication: OAuth2UserTokenExchange
          Name: devuiappsensormanager_uaa
          ServiceInstanceName: devuiappsensormanager-xsuaa-srv
          ServiceKeyName: devuiappsensormanager-uaa-key
          sap.cloud.service: devuiappsensormanager
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: devuiappsensormanager-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: devuiappsensormanager-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - devuiappsensormanager.zip
      name: devuiappsensormanager
      target-path: resources/
- name: devuiappsensormanager
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: devuiappsensormanager-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: devuiappsensormanager-destination-service
    service-plan: lite
- name: devuiappsensormanager-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: devuiappsensormanager-xsuaa-srv
    service-plan: application
- name: devuiappsensormanager-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: devuiappsensormanager-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
